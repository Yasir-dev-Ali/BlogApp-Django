# from   django.conf  import  list_display = 
from django.contrib import admin

# Import the Post model from the blogApp.models module
# from .models import Post

# Register the Post model with the admin site

# admin.site.register(Post)
# admin.site.register(Post, list_display = ['title', 'author', 'date_posted'])
# admin.site.register(Post, list_display = ['title', 'author', 'date_posted'])
# admin.site.register(Post, list_display = ['title', 'author', 'date_posted'])
# admin.site.register(Post, list_display = ['title', 'author', 'date_posted'])
